program Project1;

uses
  Vcl.Forms,
  CALCULADORA in 'CALCULADORA.pas' {TELA_CALCULADORA};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TTELA_CALCULADORA, TELA_CALCULADORA);
  Application.Run;
end.
